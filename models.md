# 모델 규약 — 규정다름 (MABC 2026 결선)

> 이 프로젝트와 관련된 모든 코딩·글쓰기·판단은 **Solar Pro 4**만 사용한다.

## 하드 가드레일

- 이 프로젝트에서 사용하는 LLM은 **Solar Pro 4로만 한정한다.**
- Claude, Codex, Gemini, Cursor, Copilot, Amp, Jules 등 다른 LLM·에이전트 플랫폼 관련 파일, 설정, 흔적은 만들지 않는다.
- `CLAUDE.md`, `GEMINI.md`, `.cursorrules`, `.github/copilot-instructions.md`, `.codex/`, `.agents/`, `.claude/`, `.gemini/` 등 벤더 특화 파일/디렉터리는 원칙적으로 만들지 않는다. 이미 존재하면 무시하거나 정리한다.
- 스킬·작업 지시·플랫폼 종속적 메타데이터는 프로젝트 관행에 필요한 최소한으로 한정한다.

## 왜 이렇게 하는가

- 이 프로젝트는 MABC 2026 결선 제출물이 목표이고, 제출물 준비와 판단 일관성은 한 모델·서술한 가드레일 아래에서 유지되어야 한다.
- 벤더별 설정 파일이 섞이면 맥락 복원 시 해석 차이가 생길 수 있다.

## 예외 처리

- 다른 모델의 흔적이 이미 존재하는 경우: 임의로 추론하지 말고, 사용자에게 확인 후 정리한다.
- 도구(웹검색, 터미널, 파일쓰기 등)는 Solar Pro 4가 이 환경에서 쓸 수 있는 범위에서 사용한다.
